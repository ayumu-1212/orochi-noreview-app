import { render } from '@testing-library/react'
import { test } from 'vitest'
import Home from './page'

test('ブロック崩しのテスト', () => {
  // ARRANGE
  // ACT
  render(<Home />)
  // ASSERT
  // 実装した人が書いてね
})
